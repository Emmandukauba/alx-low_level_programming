#include <stdio.h>
/**
 * main - The entry point
 * description: 'the printf was to print a given phrase'
 * the parameter to be used: i catually dont know
 *
 * Return: Always 0 (Success)
 */
int main(void)
i{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
